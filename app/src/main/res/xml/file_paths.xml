<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Matches Environment.DIRECTORY_DOWNLOADS inside the app's external files dir, where
         UpdateRepository tells DownloadManager to put the APK. -->
    <external-files-path
        name="update_downloads"
        path="Download/" />
</paths>
